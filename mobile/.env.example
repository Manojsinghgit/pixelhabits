# Points the app at your backend. For local development against
# `python manage.py runserver`, use your machine's LAN IP (not "localhost" —
# a physical device or simulator can't reach your laptop's localhost), e.g.
# EXPO_PUBLIC_API_URL=http://192.168.1.23:8000
EXPO_PUBLIC_API_URL=https://habittracker-api-y444.onrender.com
